name: Test

on:
#   push:
#     branches: [ "master" ]
#   pull_request:
#     branches: [ "master" ]
  workflow_dispatch:
  
jobs:

  build:
    environment: BeetBox dotenv
    runs-on: self-hosted

    steps:
      - run: whoami
      
      - run: which docker
      
      - run: id
      
      - run: docker ps
      
